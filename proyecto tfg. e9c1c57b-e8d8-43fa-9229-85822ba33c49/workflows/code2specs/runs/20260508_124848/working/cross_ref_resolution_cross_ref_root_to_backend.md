# Cross-Reference Resolution: cross_ref_root_to_backend

Generated by agent: cross_ref_root_to_backend
Type: cross_ref_resolution
Clusters: cluster_root_tests_scratch_01 ← resolved from → cluster_backend_core_01, cluster_backend_skills_01
Date: 2026-05-08T11:44:53Z

---

## RESOLUTION SUMMARY

| gap_id | gap_location | entity_resolved | resolved_in | source_file:line | confidence |
|--------|--------------|-----------------|-------------|------------------|------------|
| g_009 | GAP-cluster_root_tests_scratch_01-006 | PaperAuditor.__init__() [HIGH]; _preprocess_paper() [UNRESOLVED] | extracted_backend_core_01.md | auditor.py:28 | MEDIUM (partial) |
| g_010 | GAP-cluster_root_tests_scratch_01-007 | get_extraction_prompt(), get_evaluation_prompt() | extracted_backend_core_01.md | prompts.py:4, prompts.py:378 | HIGH |
| g_011 | GAP-cluster_root_tests_scratch_01-008 | REGEX_PATTERNS | extracted_backend_core_01.md + auditor.py direct read | auditor.py (entire file) | UNRESOLVED |
| g_012 | GAP-cluster_root_tests_scratch_01-009 | LLMClient + generate() + retry | extracted_backend_core_01.md | llm_client.py:8, llm_client.py:11, llm_client.py:30 | HIGH (from source) |
| g_014 | GAP-cluster_root_tests_scratch_01-011 | AuditState, ExtractedInfo, ChecklistItem | extracted_backend_core_01.md + backend/common/ direct read | backend/common/audit_state.py (absent) | UNRESOLVED |
| g_015 | GAP-cluster_root_tests_scratch_01-012 | InformationExtractionSkill | extracted_backend_skills_01.md | auditor_skills.py:21 | HIGH |
| g_016 | GAP-cluster_root_tests_scratch_01-013 | backend.skills.__init__ exports | extracted_backend_skills_01.md | __init__.py:36 | HIGH (15 symbols, not 12) |
| g_017 | GAP-cluster_root_tests_scratch_01-014 | LimitationsQualityDetectionSkill, SoftwareVersionDetectionSkill, HardwareDetailDetectionSkill | extracted_backend_skills_01.md | regex_detection_skills.py:564, :595, :625 | HIGH |
| g_018 | GAP-cluster_root_tests_scratch_01-015 | SemanticScholarSearchSkill.execute() | extracted_backend_skills_01.md | sota_skills.py:162 | HIGH |
| g_019 | GAP-cluster_root_tests_scratch_01-016 | get_logger() | extracted_backend_core_01.md | logger.py:44 | HIGH (from source) |
| g_020 | GAP-cluster_root_tests_scratch_01-017 | HyperparameterDetectionSkill.execute() | extracted_backend_skills_01.md | regex_detection_skills.py:135 | HIGH (from source) |
| g_023 | GAP-cluster_root_tests_scratch_01-020 | config.GOOGLE_API_KEY, MODEL_NAME | extracted_backend_core_01.md | config.py:30, config.py:107 | HIGH (from source) |

---

## From cluster_backend_core_01

### [g_009] PaperAuditor.__init__() and _preprocess_paper(text) -> dict

**GAP_ID:** g_009
**Original gap location:** GAP-cluster_root_tests_scratch_01-006
**Entity:** `PaperAuditor.__init__()` and `PaperAuditor._preprocess_paper(text)`
**Resolved from:** extracted_backend_core_01.md § 3.2 Auditor Service (auditor.py)
**Source:** `backend/services/auditor.py:28` (for `__init__`); `_preprocess_paper` — source absent
**Confidence:** MEDIUM (partial — `__init__` is HIGH, `_preprocess_paper` is UNRESOLVED)

#### `PaperAuditor.__init__(self)` — RESOLVED (HIGH)

SOURCE: `auditor.py:28` (via extracted_backend_core_01.md § 3.2)

- **Parameters:** none
- **Parent class:** none
- **Module-level logger:** `logger = get_logger(__name__)` at `auditor.py:22`

**5 `LLMClient` instances created at construction time:**
- `self.extraction_llm = LLMClient(model_name=EXTRACTION_MODEL_NAME, generation_config=AUDIT_CONFIG)` — SOURCE: `auditor.py:31`
- `self.evaluation_llm = LLMClient(model_name=EVALUATION_MODEL_NAME, generation_config=AUDIT_CONFIG)` — SOURCE: `auditor.py:34`
- `self.rag_map_llm = LLMClient(model_name=MAP_MODEL_NAME, generation_config=AUDIT_CONFIG)` — SOURCE: `auditor.py:37`
- `self.rag_reduce_llm = LLMClient(model_name=REDUCE_MODEL_NAME, generation_config=AUDIT_CONFIG)` — SOURCE: `auditor.py:40`
- `self.verification_llm = LLMClient(model_name=VERIFICATION_MODEL_NAME, generation_config=AUDIT_CONFIG)` — SOURCE: `auditor.py:43`

**6 skill instances created at construction time:**
- `self.extraction_skill = InformationExtractionSkill(llm_client=self.extraction_llm)` — SOURCE: `auditor.py:46`
- `self.hybrid_hp_skill = HybridHyperparameterExtractionSkill(llm_client=self.rag_map_llm)` — SOURCE: `auditor.py:47`
- `self.evaluation_skill = ReproducibilityEvaluationSkill(llm_client=self.evaluation_llm)` — SOURCE: `auditor.py:48`
- `self.verification_skill = ChecklistVerificationSkill(llm_client=self.verification_llm)` — SOURCE: `auditor.py:51`
- `self.metrics_skill = MetricsCalculationSkill()` (no llm_client argument) — SOURCE: `auditor.py:53`
- `self.metadata_skill = MetadataAggregationSkill()` (no llm_client argument) — SOURCE: `auditor.py:54`

**Log messages emitted at end of `__init__`:**
- `logger.info(f"✅ Motor de Embeddings inicializado: {EMBEDDING_MODEL_NAME}")` — SOURCE: `auditor.py:56`
- `logger.info("✅ Auditor inicializado respetando la configuración técnica.")` — SOURCE: `auditor.py:57`

**Return:** `None` (constructor)

#### `PaperAuditor._preprocess_paper(text)` — UNRESOLVED

**Resolution:** UNRESOLVED
**Evidence of search:**
Searched `extracted_backend_core_01.md` exhaustively: all sections (§ 3.1–3.5, §4, §5, §6, §7, §8, §9) and the full method-level decomposition under § 3.2. The method `_preprocess_paper` does not appear anywhere.
Searched source file `backend/services/auditor.py` directly: the file contains exactly two methods — `__init__` and `audit`. No `_preprocess_paper` method is defined in the current source.
The test `test_auditor_refactor.py:31` calls `auditor._preprocess_paper(text)` and asserts `isinstance(red_flags, dict)`, implying the method was expected to return a dict of detected red flags. However, the current `audit` method initialises `context = {'paper_text': paper_text, 'red_flags': {}}` with an empty `red_flags` dict (SOURCE: `auditor.py:78`), suggesting that `_preprocess_paper` was replaced by an empty placeholder during refactoring and the method body was removed.
**Recommendation:** Manual extraction from the pre-refactor commit history is required to recover the original `_preprocess_paper` contract. The current implementation no longer contains this method.

---

### [g_010] get_extraction_prompt(text, flags) -> str and get_evaluation_prompt(info, flags) -> str

**GAP_ID:** g_010
**Original gap location:** GAP-cluster_root_tests_scratch_01-007
**Entity:** `get_extraction_prompt(paper_text, red_flags) -> str` and `get_evaluation_prompt(extracted_info, red_flags) -> str`
**Resolved from:** extracted_backend_core_01.md § 2.2 prompts.py — All Prompt Templates
**Source:** `backend/common/prompts.py:4` (extraction), `backend/common/prompts.py:378` (evaluation)
**Confidence:** HIGH

#### `get_extraction_prompt(paper_text, red_flags) -> str`

SOURCE: `prompts.py:4` (via extracted_backend_core_01.md § 2.2)

**Signature:** `def get_extraction_prompt(paper_text: str, red_flags: dict) -> str`

**Parameters:**
- `paper_text`: `str` — full text of the paper; injected at end of prompt via `{paper_text}`
- `red_flags`: `dict` — two sub-uses:
  - Keys starting with `'_'` are filtered OUT from the `flags_section`; specifically `red_flags.get('_hp_snippets', {})` builds a `snippets_section` block
  - Keys NOT starting with `'_'` are rendered as the `flags_section` inline in the prompt body

**Return type:** `str` — the fully-assembled prompt string

**Return JSON schema the LLM is instructed to produce** (keys): `thought_process`, `paper_type`, `invalid_reason`, `code` {`repository_url`, `negative_phrase`, `dependencies`, `instructions`, `release_mention`}, `data` {`dataset_name`, `access_url`, `negative_phrase`, `preprocessing`, `splits`, `release_mention`}, `hyperparameters` {`optimizer`, `learning_rate`, `batch_size`, `epochs`, `training_steps`, `total_tokens`, `warmup`, `weight_decay`, `betas`, `epsilon`, `vague_phrase`, `table_reference`}, `hardware` {`gpu_cpu`, `num_gpus`, `memory`, `time`, `carbon_footprint`, `energy_consumption`, `pue`, `throughput`, `latency_metrics`}, `statistics` {`confidence_intervals`, `significance_tests`, `num_runs`}, `architecture` {`description`, `weights_available`, `release_mention`}, `baseline_comparison` {`compared_models`, `has_comparative_tables`, `same_metrics`, `results_section`}, `software_versions` {`framework`, `python_version`, `cuda_version`, `dependency_file`}, `limitations_quality` {`has_section`, `specific_points`, `quantified_issues`}, `problematic_phrases`, `theory_and_proofs` {`has_theoretical_results`, `assumptions_stated`, `proofs_included`, `appendix_reference`}, `broader_impacts_extraction` {`has_impact_statement`, `appendix_reference`, `concerns_discussed`}, `llm_usage_extraction` {`models_used_in_methodology`, `purpose_in_methodology`, `used_for_writing`, `writing_declaration_quote`}, `human_subjects_extraction` {`uses_human_annotation`, `compensation_details`, `instructions_provided`}, `licenses_extraction` {`assets_used`, `licenses_named`, `missing_licenses_for_some_assets`}, `context_mapping`

**Invalid-paper short-circuit:** If `paper_type` starts with `"INVALID"` the LLM returns only `{"paper_type": "INVALID - Not ML/AI", "invalid_reason": "explanation"}` — the function injects this guard at the top of the prompt text.

**Used by:** `InformationExtractionSkill.execute()` (outside this cluster; cross-ref `extracted_backend_skills_01.md`)

---

#### `get_evaluation_prompt(extracted_info, red_flags) -> str`

SOURCE: `prompts.py:378` (via extracted_backend_core_01.md § 2.2)

**Signature:** `def get_evaluation_prompt(extracted_info: dict, red_flags: dict) -> str`

**Parameters:**
- `extracted_info`: `dict` — serialised as `json.dumps(extracted_info, indent=2, ensure_ascii=False)` and injected into prompt body
- `red_flags`: `dict` — keys not starting with `'_'` serialised into a `flags_section` block

**Return type:** `str` — fully-assembled evaluation prompt string

**Pre-computed signals injected:** The prompt calls `get_evaluation_signals(extracted_info)` internally (SOURCE: `prompts.py:285`) to derive a `signals` dict with keys `reproducibility`, `open_access`, `statistics`, `compute_resource`, `licenses`, `crowdsourcing`; these are rendered as a `PRE-COMPUTED SIGNALS` block inside the prompt.

**Return JSON schema the LLM is instructed to produce** — 16 NeurIPS checklist items, each with sub-fields:
- `answer`: exactly `"Yes"`, `"No"`, or `"N/A"`
- `evidence`: `str` (required when `answer == "Yes"`)
- `justification`: `str` (required when `answer != "Yes"`)
- `is_no_justified`: `bool`

**Checklist item keys (16 total):** `claims`, `limitations`, `theory_assumptions_proofs`, `experimental_result_reproducibility`, `open_access_data_code`, `experimental_setting_details`, `experiment_statistical_significance`, `experiments_compute_resource`, `code_of_ethics`, `broader_impacts`, `safeguards`, `licenses`, `assets`, `crowdsourcing_human_subjects`, `irb_approvals`, `declaration_llm_usage`

**Used by:** `ReproducibilityEvaluationSkill.execute()` (outside this cluster)

---

### [g_011] REGEX_PATTERNS

**GAP_ID:** g_011
**Original gap location:** GAP-cluster_root_tests_scratch_01-008
**Entity:** `REGEX_PATTERNS` — module-level list or dict in `backend.services.auditor`, expected `len > 0`
**Resolved from:** extracted_backend_core_01.md + direct source read of `backend/services/auditor.py`
**Source:** `backend/services/auditor.py` (entire file searched)
**Confidence:** UNRESOLVED

**Resolution:** UNRESOLVED
**Evidence of search:**
Searched `extracted_backend_core_01.md` in all sections covering `backend/services/auditor.py` (§ 3.2, § 4.2, § 7, § 8): no mention of a module-level `REGEX_PATTERNS` constant anywhere.
Read source file `backend/services/auditor.py` directly (all 205 lines): the file contains module-level imports and the `PaperAuditor` class with two methods (`__init__` and `audit`). No `REGEX_PATTERNS` constant is defined at module level or within any class.
The test `test_auditor_refactor.py:18-20` executes `from backend.services.auditor import REGEX_PATTERNS` followed by `assert len(REGEX_PATTERNS) > 0`. This import would fail with `ImportError` on the current codebase, indicating the constant was removed during the refactoring that introduced the skills architecture. The regex patterns that were previously inlined in `auditor.py` now live as class-level `PATTERNS` attributes on individual skill classes in `backend/skills/regex_detection_skills.py` (e.g., `HyperparameterDetectionSkill.PATTERNS`, `SoftwareVersionDetectionSkill.PATTERNS`, etc. — SOURCE: `regex_detection_skills.py:89`, `:191`, `:237`, `:290`, `:357`, `:422`, `:458`, `:483`, `:531`, `:564`, `:595`, `:625`).
**Recommendation:** The test `test_auditor_refactor.py` references a pre-refactor API. `REGEX_PATTERNS` as a module-level symbol in `auditor.py` no longer exists. The test must be updated to reference the per-skill `PATTERNS` class attributes or the test should be retired.

---

### [g_012] LLMClient(model_name) — constructor, generate(prompt), retry logic

**GAP_ID:** g_012
**Original gap location:** GAP-cluster_root_tests_scratch_01-009
**Entity:** `LLMClient(model_name)` constructor, `LLMClient.generate(prompt)`, retry logic (6 total calls, sleep intervals)
**Resolved from:** extracted_backend_core_01.md § 3.1 LLM Client (llm_client.py); confirmed against source
**Source:** `backend/common/llm_client.py:8` (class), `:11` (constructor), `:30` (generate), `:39–42` (retry constants)
**Confidence:** HIGH (from source)

#### `LLMClient` class overview

SOURCE: `llm_client.py:8` (via extracted_backend_core_01.md § 3.1)

- **Parent class:** none
- **Module-level logger:** `logger = get_logger(__name__)` — SOURCE: `llm_client.py:6`
- **Supported provider:** Google Gemini exclusively via `google.genai.Client`
- **Pattern:** not a singleton; a new instance is created per service

---

#### `LLMClient.__init__(self, model_name=None, generation_config=None)`

SOURCE: `llm_client.py:11`

| Parameter | Type | Default | Behaviour |
|-----------|------|---------|-----------|
| `model_name` | `str \| None` | `None` | If `None`, resolved to `MODEL_NAME` from config (`"gemini-3.1-flash-lite-preview"`) |
| `generation_config` | `dict \| None` | `None` | If `None`, resolved to `{}` |

**Execution sequence:**
1. Checks `if not GOOGLE_API_KEY` (evaluates `True` if `None` or empty string) — SOURCE: `llm_client.py:19`
   - If true: calls `logger.error("ERROR: No se encontró la GOOGLE_API_KEY en el .env")` — SOURCE: `llm_client.py:20`
   - Then: raises `ValueError("No se encontró la GOOGLE_API_KEY en el .env")` — SOURCE: `llm_client.py:21`
   - Construction aborted; no attributes set
2. Creates `self.client = genai.Client(api_key=GOOGLE_API_KEY)` — SOURCE: `llm_client.py:23`
3. Sets `self.model_name = model_name or MODEL_NAME` — SOURCE: `llm_client.py:25`
4. Sets `self.generation_config = generation_config or {}` — SOURCE: `llm_client.py:26`
5. Logs `logger.info(f"✅ Cliente LLM inicializado: {self.model_name}")` — SOURCE: `llm_client.py:28`

**Return:** `None` (constructor)
**Instance attributes after successful construction:** `self.client` (genai.Client), `self.model_name` (str), `self.generation_config` (dict)

---

#### `LLMClient.generate(self, prompt)`

SOURCE: `llm_client.py:30`

**Parameter:** `prompt` — any type accepted by `genai.Client.models.generate_content` (string or structured content)
**Return type:** Google genai response object; caller accesses `.text` or `.candidates[0].content` etc.

**Retry constants** (SOURCE: `llm_client.py:39–42`):
- `max_retries = 5` — SOURCE: `llm_client.py:39`
- `base_delay = 2` (seconds) — SOURCE: `llm_client.py:40`
- Loop: `for attempt in range(max_retries + 1)` → `range(6)`, i.e., attempts 0 through 5 = **6 total attempts** — SOURCE: `llm_client.py:42`

**Inline imports at method entry:** `time`, `streamlit as st`, `random` — SOURCE: `llm_client.py:35–37`

**Per-attempt logic:**
1. Calls `self.client.models.generate_content(model=self.model_name, contents=prompt, config=self.generation_config)` — SOURCE: `llm_client.py:44–48`
2. On success: returns `response` immediately — SOURCE: `llm_client.py:49`

**Exception handling** (SOURCE: `llm_client.py:43–76`):
- Catch: `Exception as e` (broad catch, all exceptions)
- Computes `error_msg = str(e)`
- Computes `is_retryable = any(code in error_msg.upper() for code in ["503", "429", "UNAVAILABLE", "RESOURCE_EXHAUSTED", "DEADLINE_EXCEEDED"])` — SOURCE: `llm_client.py:54`

**If `attempt < max_retries AND is_retryable` (retryable, attempts remaining):**
- Computes `delay = base_delay * (2 ** attempt) + random.uniform(0, 1)` — SOURCE: `llm_client.py:58`
  - Attempt 0: `2 * 1 + jitter` ≈ 2–3s
  - Attempt 1: `2 * 2 + jitter` ≈ 4–5s
  - Attempt 2: `2 * 4 + jitter` ≈ 8–9s
  - Attempt 3: `2 * 8 + jitter` ≈ 16–17s
  - Attempt 4: `2 * 16 + jitter` ≈ 32–33s
- Logs `logger.warning(f"⚠️ Error API Gemini [{self.model_name}]: {error_msg}. Reintento {attempt + 1}/{max_retries} en {delay:.1f}s...")` — SOURCE: `llm_client.py:60`
- Attempts `st.toast(...)` inside `try/except Exception: pass` (silently ignored if Streamlit not active) — SOURCE: `llm_client.py:63–66`
- Calls `time.sleep(delay)` — SOURCE: `llm_client.py:69`
- Continues to next attempt

**If `attempt >= max_retries` (all attempts exhausted, regardless of retryability):**
- Logs `logger.error(f"❌ Error crítico tras {max_retries} reintentos: {error_msg}")` — SOURCE: `llm_client.py:73`
- `raise` (re-raises original exception) — SOURCE: `llm_client.py:76`

**If NOT `is_retryable` (non-retryable error on any attempt):**
- Logs `logger.error(f"❌ Error no reintentable detectado: {error_msg}")` — SOURCE: `llm_client.py:75`
- `raise` (re-raises original exception) — SOURCE: `llm_client.py:76`

**Retryable error codes:** HTTP 503, 429, UNAVAILABLE, RESOURCE_EXHAUSTED, DEADLINE_EXCEEDED (string match in uppercased error message)
**Non-retryable errors:** immediately re-raised (auth failures, malformed payload, etc.)

---

### [g_014] AuditState, ExtractedInfo, ChecklistItem — sub-model contracts

**GAP_ID:** g_014
**Original gap location:** GAP-cluster_root_tests_scratch_01-011
**Entity:** `AuditState`, `ExtractedInfo`, `ChecklistItem` dataclasses/models — full field contracts
**Resolved from:** extracted_backend_core_01.md + direct search of `backend/common/` directory
**Source:** `backend/common/audit_state.py` (absent from repository)
**Confidence:** UNRESOLVED

**Resolution:** UNRESOLVED
**Evidence of search:**
Searched `extracted_backend_core_01.md` in all sections (§ 1 File Index, § 2 Constants, § 3 Service Contracts, § 4 Pipelines, § 5 Security, § 6 Error Handling, § 7 Business Rules, § 8 Gaps and Cross-References, § 9 Incomplete Extractions): no mention of `AuditState`, `ExtractedInfo`, or `ChecklistItem` anywhere. The File Index (§ 1) does not list `backend/common/audit_state.py`.
Searched `extracted_backend_skills_01.md` exhaustively: no mention of these classes.
Listed files in source directory `backend/common/`: `__init__.py`, `config.py`, `llm_client.py`, `prompts.py`. No `audit_state.py` present.
The test `tests/test_audit_state.py:2` imports `from backend.common.audit_state import AuditState, ExtractedInfo, ChecklistItem`. This import would fail with `ModuleNotFoundError` on the current codebase.
From the test body, partial contracts can be inferred (but NOT confirmed as they are never executed against source):
  - `AuditState(paper_text: str)` — has fields: `paper_text` (str), `invalid_paper` (bool, default `False`), `execution_time` (float, default `0.0`); has method `to_frontend_dict() -> dict` returning a dict with keys `informacion_extraida`, `metricas`, and all checklist item keys (e.g., `claims`)
  - `AuditState(paper_text, evaluation={"claims": {...}})` — `evaluation` field (dict); `to_frontend_dict()` flattens `evaluation` keys to the top level
  - `ExtractedInfo()` — has nested sub-model field `code` with attribute `repository_url` (default `"NOT FOUND"`); has nested sub-model field `hyperparameters` with attribute `optimizer` (default `"NOT FOUND"`)
  - `ChecklistItem` — not exercised in the test; no field contracts derivable from test code
**Note:** These contracts are INFERRED from test assertions, not confirmed from source. They are marked `UNRESOLVED` because the implementing source file does not exist.
**Recommendation:** The file `backend/common/audit_state.py` is referenced but absent from the repository. Manual reconstruction or recovery from version history is required.

---

### [g_019] get_logger(name) -> logger

**GAP_ID:** g_019
**Original gap location:** GAP-cluster_root_tests_scratch_01-016
**Entity:** `get_logger(name) -> logger` with `.info()` usage
**Resolved from:** extracted_backend_core_01.md § 6.1 Logger Configuration; confirmed against source
**Source:** `backend/utils/logger.py:44` (function definition)
**Confidence:** HIGH (from source)

#### `get_logger(name) -> logging.Logger`

SOURCE: `logger.py:44` (via extracted_backend_core_01.md § 6.1 and direct source read)

**Signature:** `def get_logger(name: str) -> logging.Logger`

**Parameter:**
- `name`: `str` — typically the caller's `__name__`; passed directly to `logging.getLogger(name)`

**Return type:** `logging.Logger` instance

**Execution sequence:**
1. `logger = logging.getLogger(name)` — SOURCE: `logger.py:45`
2. `logger.propagate = False` — prevents duplicate log emission to root logger — SOURCE: `logger.py:47`
3. Guard: `if not logger.handlers:` — prevents double-handler registration on repeated calls — SOURCE: `logger.py:49`
   - Inside guard:
     - `handler = logging.StreamHandler(sys.stdout)` — output goes to stdout — SOURCE: `logger.py:49–50`
     - `handler.setFormatter(ColoredFormatter())` — SOURCE: `logger.py:50`
     - `logger.addHandler(handler)`
     - `logger.setLevel(logging.INFO)` — SOURCE: `logger.py:53`
4. Side effects always executed (even if handlers already present):
   - `logging.getLogger("google_genai").setLevel(logging.WARNING)` — SOURCE: `logger.py:56`
   - `logging.getLogger("httpx").setLevel(logging.INFO)` — SOURCE: `logger.py:57`
5. Returns `logger`

**Available logger methods (standard `logging.Logger`):** `.info(msg)`, `.debug(msg)`, `.warning(msg)`, `.error(msg)`, `.critical(msg)`

**Log format:** `"%(asctime)s - %(name)s - %(levelname)s - %(message)s"` with date format `'%H:%M:%S'` — SOURCE: `logger.py:19, :41`

**`ColoredFormatter` behaviour** (SOURCE: `logger.py:29`):
- If `"HTTP Request"` in `record.msg`: sets entire message to cyan
- Else: colors the level name based on `LEVEL_COLORS`:
  - `logging.DEBUG` → `"\033[94m"` (BLUE)
  - `logging.INFO` → `"\033[92m"` (GREEN)
  - `logging.WARNING` → `"\033[93m"` (YELLOW)
  - `logging.ERROR` → `"\033[91m"` (RED)
  - `logging.CRITICAL` → `"\033[1m\033[91m"` (BOLD + RED)

---

### [g_023] backend.common.config — GOOGLE_API_KEY and MODEL_NAME

**GAP_ID:** g_023
**Original gap location:** GAP-cluster_root_tests_scratch_01-020
**Entity:** `backend.common.config.GOOGLE_API_KEY` and `MODEL_NAME` — patchable module-level attributes
**Resolved from:** extracted_backend_core_01.md § 2.1 config.py; confirmed against source
**Source:** `backend/common/config.py:30` (GOOGLE_API_KEY), `backend/common/config.py:107` (MODEL_NAME)
**Confidence:** HIGH (from source)

#### `GOOGLE_API_KEY`

SOURCE: `config.py:30`

- **Type:** `str | None`
- **Origin:** `os.getenv("GOOGLE_API_KEY")` — reads from environment variable `"GOOGLE_API_KEY"`, which is expected to be set in a `.env` file loaded by `load_dotenv()` at `config.py:27`
- **Default:** `None` (if environment variable is not set)
- **Patchability:** Module-level variable; patchable via `unittest.mock.patch("backend.common.config.GOOGLE_API_KEY", "fake_key")` or `unittest.mock.patch("backend.common.llm_client.GOOGLE_API_KEY", "fake_key")` (because `llm_client.py` imports `GOOGLE_API_KEY` at module load time, the patch target must be the importing module's namespace to be effective in tests)
- **Usage:** Passed to `genai.Client(api_key=GOOGLE_API_KEY)` inside `LLMClient.__init__` — SOURCE: `llm_client.py:23`. If `not GOOGLE_API_KEY` evaluates `True`, `LLMClient.__init__` raises `ValueError("No se encontró la GOOGLE_API_KEY en el .env")` — SOURCE: `llm_client.py:19–21`

#### `MODEL_NAME`

SOURCE: `config.py:107`

- **Type:** `str`
- **Value:** `EXTRACTION_MODEL_NAME` (alias assignment, not a copy) — SOURCE: `config.py:107`
- **Resolved value:** `"gemini-3.1-flash-lite-preview"` (because `EXTRACTION_MODEL_NAME = "gemini-3.1-flash-lite-preview"` at `config.py:41`)
- **Origin:** Hardcoded string constant; NOT read from environment variable; NOT overridable via `.env`
- **Patchability:** Module-level variable; patchable via `unittest.mock.patch("backend.common.config.MODEL_NAME", "mock-model")` or `unittest.mock.patch("backend.common.llm_client.MODEL_NAME", "mock-model")` (same import-time-binding caveat as above)
- **Usage:** Used in `LLMClient.__init__` as the fallback model name: `self.model_name = model_name or MODEL_NAME` — SOURCE: `llm_client.py:25`; also used in `InformationExtractionSkill` REDUCE fallback call — SOURCE: `auditor_skills.py` (via extracted_backend_skills_01.md § 3.2)

**Companion constant `RAG_MODEL_NAME`** (not in gap but context):
- Value: `= MAP_MODEL_NAME` alias = `"gemini-3.1-flash-lite-preview"` — SOURCE: `config.py:108`

---

## From cluster_backend_skills_01

### [g_015] InformationExtractionSkill(llm_client) — constructor and section-fragmenting logic

**GAP_ID:** g_015
**Original gap location:** GAP-cluster_root_tests_scratch_01-012
**Entity:** `InformationExtractionSkill(llm_client)` — constructor and internal section-fragmenting logic
**Resolved from:** extracted_backend_skills_01.md § 3.2 (InformationExtractionSkill.execute)
**Source:** `backend/skills/auditor_skills.py:21`
**Confidence:** HIGH

#### Constructor

SOURCE: `auditor_skills.py` (inherits `BaseSkill.__init__` — `base_skill.py:19`)

`InformationExtractionSkill` does not define its own `__init__`; it uses the inherited `BaseSkill.__init__`:
- **Signature:** `BaseSkill.__init__(self, llm_client: Optional[LLMClient] = None, config: Optional[Dict] = None)`
- `self.llm_client = llm_client` — stored as instance attribute
- `self.config = config if config is not None else {}` — stored as instance attribute
- `self.name = self.__class__.__name__` → `"InformationExtractionSkill"` at runtime
- Side effect: `logger.debug(f"Skill 'InformationExtractionSkill' inicializado")` — SOURCE: `base_skill.py:19`

**Typical instantiation** (from `PaperAuditor.__init__`):
```python
self.extraction_skill = InformationExtractionSkill(llm_client=self.extraction_llm)
```
SOURCE: `auditor.py:46`

#### Section-Fragmenting Logic (inside `execute`)

SOURCE: `auditor_skills.py:21` (via extracted_backend_skills_01.md § 3.2)

**Input context key required:** `['paper_text']`

**Guard checks (before fragmentation):**
1. `self.validate_context(context, ['paper_text'])` — if missing key, returns `{'extracted_info': {}}`
2. `if not self.llm_client` — if no client, returns `{'extracted_info': {}}`

**Step 1 — Normalise line endings** (SOURCE: `auditor_skills.py:36`):
- `paper_text_norm = paper_text.replace('\r\n', '\n')`

**Step 2 — Split into sections** (SOURCE: `auditor_skills.py:37–38`):
- `re.split(r'\n(?=#+ )', '\n' + paper_text_norm)` — splits on Docling markdown headers (lines beginning with one or more `#` followed by a space)
- Strips each section; filters empty sections

**Step 3 — Build fragments** (SOURCE: `auditor_skills.py:40–60`):

If `len(sections) > 1` (multi-section document):
- Computes `total_chars = sum(len(s) for s in sections)`
- Computes `target = total_chars / 4` (aims for 4 balanced fragments)
- Iterates sections, accumulating `current_fragment`:
  - If `len(current_fragment) + len(section) > target AND len(fragments) < 3`: appends `current_fragment` to `fragments`, resets `current_fragment`
  - Edge case: if `current_fragment` is empty AND the section alone exceeds `target`, appends that section directly as a fragment
  - Otherwise: concatenates section with `"\n\n"` into `current_fragment`
  - After loop: appends any remaining `current_fragment`

If `len(sections) <= 1` (flat document, no headers detected):
- Fallback to `RecursiveCharacterTextSplitter(chunk_size=25000, chunk_overlap=2000)`
- Calls `.split_text(paper_text)` and takes first 4 items: `fragments[:4]`

**Step 4 — MAP phase LLM calls** (SOURCE: `auditor_skills.py:77`):
- Imports `time` inline
- For each fragment `i`: calls `get_map_extraction_prompt(fragment)` → sends to `self.llm_client.generate(prompt)`
- Reads `response.text.strip()`; extracts first complete JSON object via balanced brace counting
- Calls `json.loads(...)` → `fragment_data`; appends to `map_results`
- Between fragments (not on last): calls `time.sleep(2)` — SOURCE: `auditor_skills.py:~107`
- Catches `Exception` per fragment: logs warning, continues

**Step 5 — REDUCE phase consolidation** (SOURCE: `auditor_skills.py:113`):
- Calls `get_reduce_extraction_prompt(map_results)` → primary LLM call
- Fallback on exception: `self.llm_client.client.models.generate_content(model=REDUCE_MODEL_NAME, contents=reduce_prompt, config={"response_mime_type": "application/json", "temperature": 0.0})`
- Applies `re.sub(r',\s*([\]}])', r'\1', raw_text)` trailing-comma JSON repair on parse failure

**Return on success:**
```python
{
  'extracted_info': extracted_info,   # consolidated dict from REDUCE
  'invalid_paper': False,
  'map_steps': map_results,           # list of per-fragment dicts
  'reduce_step': extracted_info       # same object as extracted_info
}
```

**Return on invalid paper:** `{'extracted_info': extracted_info, 'invalid_paper': True, 'invalid_reason': str}`
**Return on empty MAP results:** `{'extracted_info': {}, 'extraction_error': "Map phase produced no results"}`
**Return on outer exception:** `{'extracted_info': {}, 'extraction_error': str(e)}`

---

### [g_016] backend.skills.__init__ — exported skill classes

**GAP_ID:** g_016
**Original gap location:** GAP-cluster_root_tests_scratch_01-013
**Entity:** `backend.skills.__init__` exporting skill classes
**Resolved from:** extracted_backend_skills_01.md § 1.1 Exported Symbols
**Source:** `backend/skills/__init__.py:36`
**Confidence:** HIGH

**Note on count discrepancy:** The gap detail states "12 skill classes". The verified source exports **15 symbols** (SOURCE: `__init__.py:36`, confirmed by direct count of `__all__` list at lines 36–52). The gap count of 12 is incorrect as stated.

#### Full `__all__` export list — 15 symbols

SOURCE: `backend/skills/__init__.py:36`

| # | Symbol | Origin Module |
|---|--------|---------------|
| 1 | `BaseSkill` | `backend.skills.base_skill` |
| 2 | `InformationExtractionSkill` | `backend.skills.auditor_skills` |
| 3 | `ReproducibilityEvaluationSkill` | `backend.skills.auditor_skills` |
| 4 | `MetricsCalculationSkill` | `backend.skills.auditor_skills` |
| 5 | `MetadataAggregationSkill` | `backend.skills.auditor_skills` |
| 6 | `ConversationalResponseSkill` | `backend.skills.chatbot_skills` |
| 7 | `ContextValidationSkill` | `backend.skills.chatbot_skills` |
| 8 | `ThematicCoverageSkill` | `backend.skills.sota_skills` |
| 9 | `QueryGenerationSkill` | `backend.skills.sota_skills` |
| 10 | `SemanticScholarSearchSkill` | `backend.skills.sota_skills` |
| 11 | `CoverageGapAnalysisSkill` | `backend.skills.sota_skills` |
| 12 | `CrossValidationSkill` | `backend.skills.sota_skills` |
| 13 | `LimitationsQualityDetectionSkill` | `backend.skills.regex_detection_skills` |
| 14 | `SoftwareVersionDetectionSkill` | `backend.skills.regex_detection_skills` |
| 15 | `HardwareDetailDetectionSkill` | `backend.skills.regex_detection_skills` |

**Symbols defined in the package but NOT in `__all__` (not importable via `from backend.skills import *`):**
- `ChecklistVerificationSkill` — defined in `auditor_skills.py:319`
- `HybridHyperparameterExtractionSkill` — defined in `rag_extraction_skill.py:27`
- `HyperparameterDetectionSkill`, `DataAvailabilityDetectionSkill`, `CodeAvailabilityDetectionSkill`, `StatisticsDetectionSkill`, `EnvironmentalImpactDetectionSkill`, `ProblematicPhrasesDetectionSkill`, `LlmUsageDetectionSkill`, `CrowdsourcingDetectionSkill`, `LicenseDetectionSkill` — all defined in `regex_detection_skills.py` but absent from `__all__`

**Registration logic:** None. `__init__.py` performs only `from ... import` re-exports and defines `__all__`. No registry class, factory, or dynamic registration is present.

---

### [g_017] LimitationsQualityDetectionSkill, SoftwareVersionDetectionSkill, HardwareDetailDetectionSkill

**GAP_ID:** g_017
**Original gap location:** GAP-cluster_root_tests_scratch_01-014
**Entity:** `LimitationsQualityDetectionSkill`, `SoftwareVersionDetectionSkill`, `HardwareDetailDetectionSkill` in `regex_detection_skills.py`
**Resolved from:** extracted_backend_skills_01.md § 5; confirmed against source
**Source:** `backend/skills/regex_detection_skills.py:564` (Limitations), `:595` (SoftwareVersion), `:625` (HardwareDetail)
**Confidence:** HIGH

All three classes inherit from `BaseSkill` and use `BaseSkill.__init__` (no custom constructor). They use module-level helper `_search_with_negation(pattern, text)` which applies `NEGATION_PATTERNS` to the 60-character window preceding any match.

---

#### `LimitationsQualityDetectionSkill`

SOURCE: `regex_detection_skills.py:564`

**Constructor:** Inherits `BaseSkill.__init__(self, llm_client=None, config=None)` — no LLM client used

**Class-level attributes:**

`SPECIFIC_PATTERNS` (list of 3 strings, used with `_search_with_negation`):
1. `r"(?:limitation|weakness).{0,60}(?:\d+|specific|quantif|measur|metric)"`
2. `r"(?:bias|toxicity|fairness).{0,40}(?:evaluat|measur|test|audit|analyz)"`
3. `r"(?:fail|error|degrad).{0,40}(?:when|if|under|specific|certain)"`

`SECTION_PATTERN` (used with `re.search(..., re.IGNORECASE)`):
```
r"(?:^|\n)\s*(?:#+\s*[\*_]{0,2}(?:Limitation|Broader\s+Impact|Conclusion|Discussion|Social\s+Impact)[\*_]{0,2}|(?:\*\*|__)Limitations?[\.\s:]|Limitations?[\.\s:])"
```

**`execute(self, context) -> dict`** SOURCE: `regex_detection_skills.py:571`

- **Input context key required:** `['paper_text']`
- Guard: `if not self.validate_context(context, ['paper_text']): return {}`
- Reads `text = context['paper_text']`
- `has_section = bool(re.search(self.SECTION_PATTERN, text, re.IGNORECASE))`
- `specific_count = 0`; for each pattern in `SPECIFIC_PATTERNS`: if `_search_with_negation(pat, text)` matches, increments `specific_count`
- `is_vague = has_section and specific_count == 0`
- **Return:**
```python
{'limitations_flags': {
    'tiene_seccion_limitaciones': bool,   # True if SECTION_PATTERN matched
    'limitaciones_vagas': bool,           # True if section exists but zero specific_count
    'puntos_especificos_limitaciones': int  # 0–3, count of SPECIFIC_PATTERNS matched
}}
```

---

#### `SoftwareVersionDetectionSkill`

SOURCE: `regex_detection_skills.py:595`

**Constructor:** Inherits `BaseSkill.__init__` — no LLM client used

**Class-level attribute `PATTERNS`** (list of 4 strings, all used with `_search_with_negation`):
1. `r"(?:PyTorch|TensorFlow|JAX|Keras|Transformers)\s*(?:v|version)?\s*\d+\.\d+"`
2. `r"(?:Python|CUDA|cuDNN)\s*(?:v|version)?\s*\d+\.\d+"`
3. `r"(?:numpy|scipy|pandas|scikit)\S*\s*(?:v|version)?\s*\d+\.\d+"`
4. `r"requirements\.txt|environment\.yml|setup\.py|pyproject\.toml"`

**`execute(self, context) -> dict`** SOURCE: `regex_detection_skills.py:602`

- **Input context key required:** `['paper_text']`
- Guard: `if not self.validate_context(context, ['paper_text']): return {}`
- Reads `text = context['paper_text']`
- Iterates all 4 patterns; for each: calls `_search_with_negation(pat, text)` — if match, increments `found_count` and logs snippet
- **Return:**
```python
{'software_flags': {
    'tiene_versiones_software': bool,  # True if found_count > 0
    'cantidad_versiones': int          # 0–4, total patterns matched
}}
```

---

#### `HardwareDetailDetectionSkill`

SOURCE: `regex_detection_skills.py:625`

**Constructor:** Inherits `BaseSkill.__init__` — no LLM client used

**Class-level attribute `PATTERNS`** (dict with 4 keys, each a list of 2 pattern strings, all used with `_search_with_negation` + `re.IGNORECASE`):

| Key | Pattern strings |
|-----|----------------|
| `gpu_model` | `r"(?:A100\|V100\|H100\|A6000\|RTX\s*\d{4}\|MI\d{3}\|TPU\s*v\d)"` |
| | `r"(?:NVIDIA\|AMD\|Google)\s+(?:GPU\|TPU)\s*\S+"` |
| `gpu_count` | `r"(\d+)\s*(?:x\s*)?(?:GPU\|TPU\|A100\|V100\|H100)"` |
| | `r"(?:GPU\|TPU)s?\s*(?:x\s*)?(\d+)"` |
| `gpu_memory` | `r"(\d+)\s*GB\s*(?:GPU\|VRAM\|HBM\|memory)"` |
| | `r"(?:GPU\|VRAM\|HBM)\s*(?:memory)?\s*(?:of)?\s*(\d+)\s*GB"` |
| `training_time` | `r"(?:train\|took\|required)\S*\s+(?:for\s+)?(?:approximately\s+)?\d+\s*(?:hours?\|days?\|weeks?\|GPU[\s-]hours?)"` |
| | `r"\d+\s*(?:GPU[\s-]hours?\|GPU[\s-]days?\|node[\s-]hours?)"` |

SOURCE: `regex_detection_skills.py:625`

**`execute(self, context) -> dict`** SOURCE: `regex_detection_skills.py:644`

- **Input context key required:** `['paper_text']`
- Guard: `if not self.validate_context(context, ['paper_text']): return {}`
- Reads `text = context['paper_text']`
- For each of the 4 category keys in `PATTERNS`, iterates 2 pattern strings; stops on first match per category; uses `_search_with_negation(pattern, text)` (IGNORECASE applied within that helper via compiled pattern flags)
- **Return:**
```python
{'hardware_detail_flags': {
    'tiene_gpu_model': bool,
    'tiene_gpu_count': bool,
    'tiene_gpu_memory': bool,
    'tiene_training_time': bool
}}
```

---

### [g_018] SemanticScholarSearchSkill.execute({'search_queries': [...]}) -> {'sota_papers': ...}

**GAP_ID:** g_018
**Original gap location:** GAP-cluster_root_tests_scratch_01-015
**Entity:** `SemanticScholarSearchSkill.execute({'search_queries': [...]}) -> {'sota_papers': ...}` — full input/output contract
**Resolved from:** extracted_backend_skills_01.md § 6.2 (SemanticScholarSearchSkill.execute)
**Source:** `backend/skills/sota_skills.py:162`
**Confidence:** HIGH

#### Constructor

Inherits `BaseSkill.__init__(self, llm_client=None, config=None)` — no LLM client used for this skill; it uses HTTP requests to the Semantic Scholar API directly.

#### `SemanticScholarSearchSkill.execute(self, context) -> dict`

SOURCE: `sota_skills.py:162` (via extracted_backend_skills_01.md § 6.2)

**Input context key required:** `['search_queries']`

**Guard checks:**
1. `self.validate_context(context, ['search_queries'])` — if missing, returns `{}` (empty dict via `validate_context` failure path)
2. `queries = context['search_queries']`; `if not queries: return {'sota_papers': []}`

**Configuration (from `backend.common.config`, cross-referenced):**
- `SEMANTIC_SCHOLAR_API_KEY` — optional; if set, adds header `"x-api-key": SEMANTIC_SCHOLAR_API_KEY`
- `SEMANTIC_SCHOLAR_BASE_URL` = `"https://api.semanticscholar.org/graph/v1/paper/search"` — SOURCE: `config.py:136`
- `SEMANTIC_SCHOLAR_YEAR_RANGE` = `"2023-2026"` — SOURCE: `config.py:137`
- `SEMANTIC_SCHOLAR_LIMIT` = `5` — SOURCE: `config.py:138`
- `SEMANTIC_SCHOLAR_FIELDS` = `"paperId,title,authors,year,citationCount,abstract,url"` — SOURCE: `config.py:139`

**Per-query execution:**
- Iterates `queries` with index `i`; sleeps `0.5` seconds between queries (NOT before the first query)
- Per query: `requests.get(SEMANTIC_SCHOLAR_BASE_URL, params={'query': q, 'year': SEMANTIC_SCHOLAR_YEAR_RANGE, 'limit': SEMANTIC_SCHOLAR_LIMIT, 'fields': SEMANTIC_SCHOLAR_FIELDS}, headers=headers, timeout=15)`
- HTTP 200: extends `sota_papers` list with `response.json().get("data", [])`; logs count; logs warning if count == 0
- HTTP 429: logs rate limit warning; calls `time.sleep(2)`
- Other status codes: logs warning with status code; continues
- Exception: logs error; continues to next query

**Post-loop deduplication and ranking:**
- Deduplication: `{p['paperId']: p for p in sota_papers if p.get('paperId')}.values()`
- Sorts by `citationCount` descending
- Takes top 10: `sorted_papers[:10]`

**Return:**
```python
{'sota_papers': list}
```
- Each element is a dict with fields from `SEMANTIC_SCHOLAR_FIELDS`: `paperId`, `title`, `authors`, `year`, `citationCount`, `abstract`, `url`
- Maximum 10 items; deduplicated by `paperId`; sorted by `citationCount` descending
- Returns `{'sota_papers': []}` if `queries` is empty or all API calls fail

**External API dependency:** `https://api.semanticscholar.org/graph/v1/paper/search` (Semantic Scholar public API); requires network access; rate-limited (HTTP 429 mitigation: 2s sleep)

---

### [g_020] HyperparameterDetectionSkill.execute({'paper_text': str}) -> dict

**GAP_ID:** g_020
**Original gap location:** GAP-cluster_root_tests_scratch_01-017
**Entity:** `HyperparameterDetectionSkill.execute({'paper_text': str}) -> dict` (JSON-serializable output shape)
**Resolved from:** extracted_backend_skills_01.md § 5; confirmed against source
**Source:** `backend/skills/regex_detection_skills.py:135`
**Confidence:** HIGH (from source)

#### Constructor

Inherits `BaseSkill.__init__(self, llm_client=None, config=None)` — no LLM client used

#### Class-level attribute `PATTERNS` (dict)

SOURCE: `regex_detection_skills.py:89` (via extracted_backend_skills_01.md § 5.2)

Keys and pattern lists (all IGNORECASE):

| Key | Pattern strings |
|-----|----------------|
| `optimizer` | `r"(?:we\s+use\|using\|with)\s+(?:the\s+)?(AdamW\|Adam\|SGD\|RMSprop\|Adagrad\|LAMB\|LARS)\s*(?:optimizer)?"` |
| | `r"optimizer[:\s=]+\s*(AdamW\|Adam\|SGD\|RMSprop\|LAMB)"` |
| | `r"(AdamW\|Adam\|SGD\|RMSprop\|LAMB)\s+optimizer"` |
| `learning_rate` | `r"learning\s+rate.{0,20}(?:is\|of\|at)?\s*([\d\.e\-×\^]+)"` |
| | `r"(?:LR\|lr)\s*[:=]\s*([\d\.e\-×\^]+)"` |
| | `r"learning\s+rate\s+is\s+linearly\s+increased\s+from\s+0\s+to\s+([\d\.e\-×\^]+)"` |
| | `r"rate\s+of\s+([\d\.e\-]+)"` |
| `batch_size` | `r"(?:batch[\s_-]size\|global[\s_-]batch\|micro[\s-]?batch)[\s:=]+.{0,20}(\d{2,})"` |
| | `r"(\d{2,})\s*(?:samples?\|examples?)\s*per\s*(?:batch\|GPU)"` |
| `epochs` | `r"(?:trained?\s+for\|over)\s+(\d+)\s*(?:epochs?\|steps?\|iterations?)"` |
| | `r"(?:epochs?\|training[\s_-]steps?)[\s:=]+(\d+)"` |
| | `r"(\d+)\s*(?:epochs?\|training\s+steps)"` |
| `warmup` | `r"warmup[\s:=]+(\d+)"` |
| | `r"(\d+)\s*warmup\s*steps?"` |
| | `r"warm[\s-]?up.{0,20}(?:of\|for\|=\|:)\s*(\d+)"` |
| | `r"Warmup-Stable-Decay"` |
| `weight_decay` | `r"(?:weight[\s_-]decay\|\$\\lambda\$\|L2[\s_-]regularization)[\s:=,]+.{0,30}(\d+\.\d+)"` |
| | `r"(?:decay\|wd)\s*[=:]\s*(\d+\.\d+)"` |
| | `r"(?:weight.decay\|regularization)\s+(?:of\|is\|coefficient)\s+(\d+\.\d+)"` |
| `betas` | `r"(?:\$\\beta_[12]\$\|beta[\s_-]?[12]\|betas?)[\s:=,]+.{0,30}\d+\.\d+"` |
| | `r"\(\s*(?:β\|beta)?\s*0\.9\d*\s*,\s*0\.9\d*\s*\)"` |
| | `r"(?:momentum\|beta)\s+(?:coefficients?\|parameters?).{0,40}\d+\.\d+"` |
| `epsilon` | `r"(?:epsilon\|\$\\epsilon\$\|eps)[\s:=]+(\d+\.?\d*[eE][-+]?\d+)"` |
| | `r"(\d+\.\d+[eE][-+]?\d+).{0,20}(?:epsilon\|numerical[\s_-]stability)"` |
| `vague` | `r"(?:standard\s+settings\|default\s+parameters\|typical\s+configuration\|not\s+disclosed\|internal\s+experimentation\|cannot\s+be\s+disclosed\|hyperparameters?\s+(?:were\s+)?(?:not\s+)?tuned)"` |

#### `execute(self, context) -> dict`

SOURCE: `regex_detection_skills.py:135` (confirmed against source)

**Input context:** `{'paper_text': str}` (key `'paper_text'` required)
**Guard:** `if not self.validate_context(context, ['paper_text']): return {}`

**Execution logic:**
1. Reads `text = context['paper_text']`
2. Extracts tables: `tables = TableExtractionHelper.extract_tables(text)` → `table_text = '\n'.join(tables)` — SOURCE: `regex_detection_skills.py:141`
3. For each `key` in `self.PATTERNS` (8 keys: `optimizer`, `learning_rate`, `batch_size`, `epochs`, `warmup`, `weight_decay`, `betas`, `epsilon`, `vague`):
   - PRIORITY 1: if `table_text` non-empty, searches tables WITHOUT negation filtering: `re.search(pattern, table_text, re.IGNORECASE)` — SOURCE: `regex_detection_skills.py:150–157`; if match → `found=True`, `matched_snippet = match.group(0)[:100]`, `search_location="TABLE"`; breaks inner pattern loop
   - PRIORITY 2: searches full text WITH negation filter: `_search_with_negation(pattern, text)` — SOURCE: `regex_detection_skills.py:160–166`; if match → `found=True`, `matched_snippet=match.group(0)[:100]`, `search_location="TEXT"`; breaks
   - Stores per key: `results[f'has_{key}'] = found`; if `found and key != 'vague' and matched_snippet`: stores `results[f'{key}_value'] = matched_snippet`, `results[f'{key}_location'] = search_location`

**Return type:** `dict` (JSON-serializable — all values are `bool`, `str`, or absent)
```python
{'hyperparameter_flags': {
    # Boolean presence flags (9 keys):
    'has_optimizer': bool,
    'has_learning_rate': bool,
    'has_batch_size': bool,
    'has_epochs': bool,
    'has_warmup': bool,
    'has_weight_decay': bool,
    'has_betas': bool,
    'has_epsilon': bool,
    'has_vague': bool,

    # Matched snippet (only when found=True AND key != 'vague'), up to 100 chars:
    'optimizer_value': str,       # optional
    'learning_rate_value': str,   # optional
    'batch_size_value': str,      # optional
    'epochs_value': str,          # optional
    'warmup_value': str,          # optional
    'weight_decay_value': str,    # optional
    'betas_value': str,           # optional
    'epsilon_value': str,         # optional
    # (no 'vague_value' — intentionally omitted for the 'vague' key)

    # Match location (only when found=True AND key != 'vague'):
    'optimizer_location': str,      # "TABLE" or "TEXT"
    'learning_rate_location': str,  # optional
    'batch_size_location': str,     # optional
    'epochs_location': str,         # optional
    'warmup_location': str,         # optional
    'weight_decay_location': str,   # optional
    'betas_location': str,          # optional
    'epsilon_location': str,        # optional
}}
```

SOURCE: `regex_detection_skills.py:177` (result keys), confirmed against source at `:135–195`

---
