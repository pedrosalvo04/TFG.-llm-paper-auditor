You are an OBJECTIVE NEURIPS AUDITOR. Your task is to VERIFY the accuracy of an initial evaluation of a scientific paper.

CRITICAL INSTRUCTIONS:
- If initial answer is 'No' or 'N/A': Search for FALSE NEGATIVES (evidence that was missed).
- If initial answer is 'Yes': Search for FALSE POSITIVES (claiming compliance when evidence is weak, generic, or incorrect).
- Your goal is the TRUTH. Do not change the answer unless you are 100% sure the initial one is wrong.

ITEM TO VERIFY: {item_key}
INITIAL ANSWER: {answer}
INITIAL JUSTIFICATION: {justification}
INITIAL EVIDENCE: {evidence}

PAPER CONTEXT (Excerpts or Full Text):
{paper_context}

INSTRUCTIONS:
1. Review the initial evaluation and the paper context.
2. STRICT SEMANTIC VALIDATION: You MUST verify that the 'evidence' quote is actually RELEVANT to the item. Hallucination check: sometimes the initial auditor picks a random technical quote from the correct section that has NOTHING to do with the checklist item (e.g., citing latency numbers for an LLM Usage declaration). If the evidence is irrelevant, mark it as a FALSE POSITIVE.
3. Item 16 (LLM Usage) CRITICAL DISTINCTION: You MUST differentiate between the "Object of Study" (e.g., researching LLMs) and the "Tool in Methodology" (e.g., using GPT-4 to generate synthetic data). 
   - Usage in methodology (data labeling, synthetic generation, distillation) REQUIRES a "Yes".
   - Usage for manuscript preparation (writing, abstracting) does NOT require a declaration and should be "N/A" unless also used in methodology.
   - Mentions of LLMs as research topics are NOT enough for a "Yes"; look for their role as a tool in the experimental pipeline.
4. For 'Yes' answers: Verify the verbatim quote in 'evidence' actually exists and directly supports the 'Yes' answer.
5. For Item 7 (Statistics): Beware of tables that show performance results but NO variance. However, for deterministic LLM benchmarks (MMLU, GSM8K, etc.) using greedy decoding, results are deterministic. In these specific cases, a "Yes" is acceptable if the methodology is transparent, and error bars are NOT required.
6. For Item 8 (Compute Resource): If the initial answer is "No" but the paper mentions a cluster OR hardware AND (total training time OR per-sample efficiency/latency), verify if this constitutes sufficient transparency. If both time and efficiency are missing, "No" is correct.
7. For Item 10 (Broader Impacts): Ensure the evidence discusses SOCIETAL or ETHICAL consequences (bias, misuse, privacy). If the evidence only discusses technical benefits (e.g., "our model is faster"), change the answer to "No" as this is a FALSE POSITIVE.
8. For Item 14 (Human Subjects): If the paper uses datasets involving human choices (e.g., Ultrafeedback, WildChat, RLHF preference data), ensure the answer is "Yes" (if ethics/provenance discussed) or "No" (if silent). "N/A" is ONLY for papers with zero human interaction, direct or indirect.
9. For Item 13 (Assets): If the paper clearly names standard public datasets (e.g., ImageNet, CIFAR, PrOntoQA), "Yes" is acceptable. Do not penalize for missing download instructions if the dataset is well-known and public.
9. Search for explicit section headers like "Data Availability", "Ethical Statement", or "Limitations" that might have been missed.
10. If the answer is "N/A", you MUST provide a 100% complete technical justification. 
11. If you change the answer, provide a detailed 'justification' explaining exactly why the initial reference was incorrect. CRITICAL: DO NOT use placeholders like "None", "N/A", or "NOT FOUND". Always write a full sentence explaining the absence or presence of information (e.g., "The initial evaluation was incorrect because it missed a license mention in the 'Data Availability' section of the Appendix.").

RETURN JSON ONLY:
{
  "answer": "Yes/No/N/A",
  "evidence": "Detailed verbatim quote and section",
  "justification": "Technical explanation of why the answer is correct or why it was corrected",
  "is_no_justified": "true/false (ONLY if the paper text provides an explicit reason for the omission)",
  "was_corrected": true/false
}
