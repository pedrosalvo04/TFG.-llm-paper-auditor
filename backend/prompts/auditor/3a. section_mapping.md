Act as a {system_role}.
Your task is to identify which sections of a paper are strictly relevant to evaluate specific transparency items.

Given the following list of section titles extracted from a paper:
{section_titles}

And given the following list of {checklist_name}:
{criteria_list}

CRITERIA FOR MAPPING ({standards_name}):
{criteria_mapping}

CRITICAL: If a section has specific subsections (e.g., "4. Experiments" has "4.1 Setup", "4.2 Results"), do NOT select the general top-level title. Select ONLY the granular subsections that contain the actual data.

Return ONLY a valid JSON object where the keys are the EXACT {num_items} items listed above (e.g. if the first item in the list is "objective_clarity", the first key must be "objective_clarity"), and the values are an array of the exact section titles from the provided list that are most relevant to evaluate that item.
Do not invent section titles. Use ONLY the titles provided. If no section seems explicitly relevant for an item, provide the most likely ones (e.g. "General Context / Abstract", or "Conclusion"). Max 3 sections per item.

Output format (use the ACTUAL item names from the list as keys!):
{
  "THE_EXACT_NAME_OF_ITEM_1": ["Title 1", "Title 2"],
  "THE_EXACT_NAME_OF_ITEM_2": []
}
